# CasaOS / ZimaOS AppStore Source Protocol v2 模板
# 规范来源：IceWhaleTech/CasaOS-AppStore/docs/guides/third-party-store-guide.md
# 构建：python3 scripts/build_appstore.py --source . --output dist --base-url <cdn>
name: rinadown
services:
  rinadown-server:
    # 规则：禁止使用 latest，须钉具体版本。发布新版时同步更新此处与 x-casaos.version。
    image: ghcr.io/zerx-lab/rinadown-server:0.2.3
    container_name: rinadown-server
    restart: unless-stopped
    ports:
      - target: 17800
        published: "17800"
        protocol: tcp
    volumes:
      - type: bind
        source: /DATA/AppData/$AppID/data
        target: /data
      - type: bind
        source: /DATA/Downloads
        target: /root/Downloads
    environment:
      TZ: $TZ
    # 可选：接外部 PostgreSQL
    # RINADOWN_DATABASE_URL: postgres://user:pass@host:5432/rinadown
x-casaos:
  id: dev.zerx.rinadown
  app_id: dev.zerx.rinadown
  # --- Access entry ---
  main: rinadown-server
  index: /
  port_map: "17800"
  scheme: http
  # --- Display ---
  title:
    en_US: RinaDown
    zh_CN: RinaDown
  icon: https://cdn.jsdelivr.net/gh/zerx-lab/RinaDown@main/assets/logo/rinadown_logo.svg
  tagline:
    en_US: Downloads, Supercharged.
    zh_CN: 下载，全面加速。
  description:
    en_US: >-
      A blazing fast, multi-protocol download manager powered by a Rust engine.
      HTTP/HTTPS/FTP/BitTorrent/HLS with intelligent segmentation, resume, speed
      control, browser extension, aria2-compatible JSON-RPC and an MCP endpoint.
      (alternative to Internet Download Manager, aria2)
    zh_CN: >-
      Rust 引擎驱动的高速多协议下载管理器。支持 HTTP/HTTPS/FTP/BitTorrent/HLS，
      智能分段、断点续传、限速、浏览器扩展，兼容 aria2 JSON-RPC 并内置 MCP 端点。
  tips:
    before_install:
      en_US: >-
        On first launch, open the Web UI at port 17800 and create your own access
        key (8+ characters, letters and digits). It is required by the Web UI, the
        management API and the MCP endpoint. Unattended installs can preset it with
        the RINADOWN_TOKEN environment variable.
      zh_CN: >-
        首次启动后打开 17800 端口的 Web 界面，自行设置访问密钥（至少 8 位，
        必须同时包含字母和数字）。Web 界面、管理 API 与 MCP 端点都需要它；
        无人值守安装可用 RINADOWN_TOKEN 环境变量预置。
  # --- Metadata ---
  author: zerx-lab
  developer: zerx-lab
  category: Downloader
  architectures:
    - amd64
    - arm64
  # --- Version ---
  version: "0.2.3"
  update_at: "2026-07-27"
  release_notes:
    en_US: See https://github.com/zerx-lab/RinaDown/releases
    zh_CN: 见 https://github.com/zerx-lab/RinaDown/releases
  # --- Links ---
  website: https://rinadown.zerx.dev
  repo: https://github.com/zerx-lab/RinaDown
  support: https://github.com/zerx-lab/RinaDown/issues
  docs: https://rinadown.zerx.dev
